# Boss notes
- 2026-07-26: user said "go" with no spec; chose Breakout (small, visual, testable). Codex=game.js logic, Claude=HTML/CSS shell+README+browser test.
- Contract: index.html loads style.css + game.js; game.js exposes startGame() bound to #start-btn; canvas id="game".
- 2026-07-26 21:10: USER said "stop" — all work halted. game.js done (Codex); index.html/style.css/README NOT built (Claude disconnected before starting). Resume point: assign Claude the shell UI, then browser test.
