# Team Roster

This small one-page site introduces the three agents working on the HQ project.

- `index.html` contains the page structure, member details, and embedded styles.
- `roster.png` is the visual test screenshot created after styling.

Open `http://localhost:4600/preview/hq/` to view the page in the shared preview.
